<div id="<%= dom_id post %>">
  <div class="card" style="width: 18rem;">
    <%= image_tag(post.main_image, class: "card-img-top", alt: post.title) %>
    <div class="card-body">
      <h2 class="card-title"><%= post.title %></h2>
      <p class="card-text"><%= post.body %></p>
      <%= link_to "Czytaj więcej ->", post, class: "btn" %>
    </div>
  </div>
</div>
